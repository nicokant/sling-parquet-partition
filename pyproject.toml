[project]
name = "sling-parquet-metadata-issue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.3.2",
    "pyarrow>=21.0.0",
    "sling>=1.4.19",
]
